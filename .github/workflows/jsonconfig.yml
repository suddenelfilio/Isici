name: .NET Core

on:
  push:
    branches: [ master ]
    paths: ./src/Isici.Configuration.JsonFileConfiguration
  pull_request:
    branches: [ master ]
    paths: ./src/Isici.Configuration.JsonFileConfiguration

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --project ./src/Isici.Configuration.JsonFileConfiguration/Isici.Configuration.JsonFileConfiguration.csproj --configuration Release --no-restore
